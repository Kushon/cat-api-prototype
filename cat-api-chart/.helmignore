# Patterns to ignore when building packages.
# This supports shell glob patterns, relative path patterns, and negated patterns
# (starting with '!'). Only one pattern per line.
.DS_Store
# Common VCS dirs
.git/
.gitignore
.bzr/
.bzrignore
.hg/
.hgignore
.svn/
# Common backup files
*.swp
*.swo
*~
.idea/
*.tmproj
.vscode/
*.iml
# Patterns for Helm, we want to ignore extra files generated from the Chart
Chart.lock
